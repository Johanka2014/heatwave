document.addEventListener('DOMContentLoaded', () => {
    // This is where you could add interactivity, for example, for the bingo game.
    // For now, this script ensures the DOM is loaded before any potential future scripts run.
    console.log("Website is ready.");
});
